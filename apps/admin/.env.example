# ERP public origin for admin "Impersonate tenant" (opens /erp/impersonate in a new tab).
# Use scheme + host (+ port in dev). Do not include /erp unless that is the full app base (see TenantDetailPage helper).
# Examples:
#   Dev:  http://localhost:3060
#   Prod: https://app.example.com
VITE_ERP_URL=http://localhost:3060
